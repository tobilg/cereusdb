{
  "name": "@cereusdb/documentation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd ../.. && cd js && npm exec tsc && cd ../packages/documentation && typedoc",
    "build:typedoc": "typedoc",
    "build:release": "typedoc",
    "preview": "npx serve dist",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "typedoc": "^0.28.18",
    "typescript": "^5.9.3",
    "wrangler": "^4.81.1"
  }
}
